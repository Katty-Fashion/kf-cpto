---
phase: quick-260626-b6m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/aggregator.py
  - docs/_includes/sidebar.html
autonomous: true
requirements: [QUICK-260626-b6m]
must_haves:
  truths:
    - "Project page links resolve (no 404) by lowercasing the /projects/:name/ path while keeping visible labels original-case"
    - "LOE report no longer renders an Effort by Assignee section"
    - "Unified calendar shows a computed Effort by Project pie (one slice per non-zero project), not the static CPTO-50h allocation pie"
    - "Per-project pages no longer render an Effort by Status pie"
    - "A new Agile Sprints page renders a sprint-timeline gantt and a sprint-summary table with a portfolio TOTAL row, reachable from the sidebar"
  artifacts:
    - path: "scripts/aggregator.py"
      provides: "generate_agile_sprints() plus edited kanban/calendar/loe/project-page generators"
      contains: "def generate_agile_sprints"
    - path: "docs/_includes/sidebar.html"
      provides: "Agile Sprints nav link"
      contains: "agile-sprints.html"
  key_links:
    - from: "scripts/aggregator.py main()"
      to: "docs/agile-sprints.md"
      via: "write_text(generate_agile_sprints(data))"
      pattern: "agile-sprints\\.md"
---

<objective>
Batch of dashboard rendering fixes plus one new page, all in `scripts/aggregator.py` (and one sidebar edit). Charts are Mermaid; all data comes from the already-parsed `data` dict — no new parser, no `loe.yml` change.

Purpose: Fix 404 project links, remove redundant/static charts, add per-project effort visibility on the aggregated view, and add an Agile Sprints page.

Output: Edited `scripts/aggregator.py`, edited `docs/_includes/sidebar.html`. At runtime, a new `docs/agile-sprints.md` is generated by the pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Key helpers (already imported in aggregator.py) — use directly, no exploration needed. -->

From scripts/aggregator.py:
- `_md_cell(value: str) -> str` — escapes free text for Markdown table cells (&, <, >, |, newlines).
- `_ISO_DATE_RE` — compiled regex; `.match(s)` is truthy only for `YYYY-MM-DD`. Use to gate gantt date cells (placeholders like "—"/"-"/"TBD" must be skipped).
- `_render_kanban_board(statuses, *, link_project=True)` — unrelated to this plan.

From scripts/utils.py (imported into aggregator.py):
- `parse_effort_days(effort: str) -> float` — parses "Nd"/effort strings to days (0 on unparseable).
- `mermaid_gantt_label(text: str) -> str` — safe label for gantt titles/sections (strips `:`, `;`, quotes, collapses whitespace). Use for ALL gantt section/bar labels.
- `mermaid_label_safe(text: str) -> str` — neutralizes `"`/`[`/`]`/newlines. Use for pie-slice labels (which are double-quoted).

Project data shape (`data: dict`): `{ project_name: { "meta": {sprint, sprint_start, sprint_end, ...}, "tasks": [ {task, assignee, effort, status, start, end}, ... ] } }`. Statuses include "Done", "In Progress", "Review", "Todo" (see `TASK_STATUSES`).

Existing gantt pattern (reuse in TASK 4) — generate_unified_kanban Sprint Timeline (aggregator.py ~183-199) and generate_unified_calendar (~237-248): gate each project on `_ISO_DATE_RE.match(s_start) and _ISO_DATE_RE.match(s_end)`, emit `    section {mermaid_gantt_label(project)}` then `    {mermaid_gantt_label(sprint)} :active, {s_start}, {s_end}`. Gantt header: `dateFormat YYYY-MM-DD`, `excludes weekends`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lowercase /projects/:name/ link paths (fix 404s)</name>
  <files>scripts/aggregator.py</files>
  <action>
Jekyll lowercases the `:name` permalink, so `/projects/R3-AAS/` 404s while the live page is `/projects/r3-aas/`. Fix the two href builders; keep all visible labels original-case.

(1) In `_render_kanban_board` (aggregator.py ~108), the board card href is built as `"{{{{ '/projects/{proj}/' | relative_url }}}}".format(proj=project)`. Change the interpolated path segment to `project.lower()` so the rendered Liquid is `/projects/r3-aas/`. Keep `escaped_project` (the visible card prefix) using the original-case `project` — do NOT lowercase the visible text.

(2) In `generate_project_page` `deps_display` (aggregator.py ~342-344), each dependency is `f"[{d}]({{{{ '/projects/{d}/' | relative_url }}}})"`. Compute a lowercase copy of the dep for the href path only: keep the visible link label `[{d}]` original-case, and use the lowercased value inside the `'/projects/{...}/'` path. (e.g. via a per-item local like `d_lower = d.lower()`.)

Do NOT touch `builder_link` (aggregator.py ~350) — its `?project=` query value is consumed by JS and MUST stay original-case.
  </action>
  <verify>
    <automated>cd /Users/machina/Dev/kf-cpto &amp;&amp; python -c "
import sys; sys.path.insert(0,'scripts')
import aggregator as a
data={'R3-AAS':{'meta':{'sprint':'S1','depends_on':['kf-platform']},'tasks':[{'task':'t','assignee':'x','effort':'2d','status':'Todo','start':'','end':''}]},'kf-platform':{'meta':{'sprint':'S1','depends_on':[]},'tasks':[{'task':'u','assignee':'y','effort':'1d','status':'Todo','start':'','end':''}]}}
k=a.generate_unified_kanban(data)
assert \"/projects/r3-aas/\" in k, 'kanban href not lowercased'
assert 'R3-AAS:' in k, 'visible card label lost original case'
p=a.generate_project_page('R3-AAS', data['R3-AAS'])
assert \"/projects/kf-platform/\" in p, 'deps href not lowercased'
assert '[kf-platform]' in p, 'deps visible label lost original case'
assert '?project=R3-AAS' in p, 'builder_link project query wrongly altered'
print('T1 OK')
"</automated>
  </verify>
  <done>Kanban board hrefs and project-page dependency hrefs use lowercase project paths; visible labels and the builder `?project=` query stay original-case.</done>
</task>

<task type="auto">
  <name>Task 2: Remove "Effort by Assignee" from generate_loe_report</name>
  <files>scripts/aggregator.py</files>
  <action>
In `generate_loe_report` (aggregator.py ~253-319), delete the entire "## Effort by Assignee" section: the blank line + `## Effort by Assignee` header, the table header/separator lines, the `assignee_data` aggregation loop, and its row-emitting loop (~lines 295-317). The function must end immediately after the Summary-by-Project `**Total**` row (aggregator.py ~293) followed by the `return "\n".join(lines)`. Leave the entire "Summary by Project" section and its Total row intact.
  </action>
  <verify>
    <automated>cd /Users/machina/Dev/kf-cpto &amp;&amp; python -c "
import sys; sys.path.insert(0,'scripts')
import aggregator as a
data={'kf-platform':{'meta':{'sprint':'S1'},'tasks':[{'task':'t','assignee':'x','effort':'2d','status':'Done','start':'','end':''}]}}
r=a.generate_loe_report(data)
assert 'Effort by Assignee' not in r, 'Effort by Assignee section still present'
assert '## Summary by Project' in r, 'Summary by Project section missing'
assert '**Total**' in r, 'Total row missing'
print('T2 OK')
"</automated>
  </verify>
  <done>`generate_loe_report` output contains no "Effort by Assignee" text; "Summary by Project" and its Total row remain.</done>
</task>

<task type="auto">
  <name>Task 3: Effort-per-project pie on calendar; drop per-project effort pie</name>
  <files>scripts/aggregator.py</files>
  <action>
(A) In `generate_unified_calendar` (aggregator.py ~204-250): replace the static pie block. Remove the `> CPTO 50h Monthly Allocation` blurb line and the entire fenced block `pie title Alocarea Lunara 50 Ore — CPTO KF` with its 6 hardcoded slices (~lines 214-224). In its place, compute per-project total effort: for each project, `sum(parse_effort_days(t["effort"]) for t in project_data["tasks"])`. Build a list of `(project, total)` pairs, skip any with `total <= 0`, sort descending by total. Emit a Mermaid pie:
- ```` ```mermaid ````
- `pie title Effort by Project (person-days)`
- one line per project: `    "{mermaid_label_safe(project)}" : {total}` (pie slice labels are double-quoted; use `mermaid_label_safe` to neutralize quotes/brackets).
- close the fence.
Keep the blank-line spacing consistent with the surrounding list+join style. The "## Sprint Calendar" gantt section below (and its per-project sections loop) stays unchanged. If no project has non-zero effort, omit the pie fence entirely (do not emit an empty pie).

(B) In `generate_project_page` (aggregator.py ~489-505): delete the entire per-project "## Effort Distribution" / `pie title Effort by Status` block, including the `effort_by_status` aggregation dict that feeds only it. Per decision, effort-per-project lives only on the aggregated calendar view. Leave the Sprint Timeline gantt and all other sections intact.
  </action>
  <verify>
    <automated>cd /Users/machina/Dev/kf-cpto &amp;&amp; python -c "
import sys; sys.path.insert(0,'scripts')
import aggregator as a
data={'R3-AAS':{'meta':{'sprint':'S1','sprint_start':'2026-07-01','sprint_end':'2026-07-10'},'tasks':[{'task':'t','assignee':'x','effort':'3d','status':'Todo','start':'','end':''}]},'kf-platform':{'meta':{'sprint':'S1'},'tasks':[{'task':'u','assignee':'y','effort':'5d','status':'Done','start':'','end':''}]},'empty':{'meta':{'sprint':'S1'},'tasks':[{'task':'z','assignee':'q','effort':'0d','status':'Todo','start':'','end':''}]}}
c=a.generate_unified_calendar(data)
assert 'pie title Effort by Project' in c, 'computed pie missing'
assert 'Alocarea Lunara' not in c and 'CPTO 50h' not in c and 'CPTO KF' not in c, 'static allocation pie still present'
# descending order: kf-platform(5) before R3-AAS(3); empty(0) skipped
assert c.index('kf-platform') < c.index('R3-AAS'), 'slices not sorted descending'
assert '\"empty\"' not in c, 'zero-effort project not skipped'
assert '## Sprint Calendar' in c, 'gantt section removed'
p=a.generate_project_page('R3-AAS', data['R3-AAS'])
assert 'pie title Effort by Status' not in p and 'Effort Distribution' not in p, 'per-project effort pie still present'
print('T3 OK')
"</automated>
  </verify>
  <done>Calendar renders a computed "Effort by Project" pie sorted descending with zero-effort projects skipped and no static CPTO allocation pie; per-project pages have no Effort Distribution pie; the Sprint Calendar gantt is unchanged.</done>
</task>

<task type="auto">
  <name>Task 4: New Agile Sprints page (timeline + summary) wired into pipeline + sidebar</name>
  <files>scripts/aggregator.py, docs/_includes/sidebar.html</files>
  <action>
(A) Add `def generate_agile_sprints(data: dict) -> str:` to aggregator.py (place it near the other generators, e.g. after `generate_loe_report`). Build with the list + `"\n".join(lines)` convention. Frontmatter: `---`, `title: Agile Sprints`, `generated: {datetime.now().isoformat()}`, `---`, blank, `# KF Team — Agile Sprints`, blank.

`## Sprint Timeline` — a Mermaid gantt mirroring the generate_unified_kanban Sprint Timeline pattern: fence `gantt`, `    title Sprint Timeline`, `    dateFormat YYYY-MM-DD`, `    axisFormat %d %b`, `    excludes weekends`, blank. For each `project, project_data in data.items()`: read `meta = project_data.get("meta", {})`, `s_start`/`s_end` stripped from meta; gate on `_ISO_DATE_RE.match(s_start) and _ISO_DATE_RE.match(s_end)`. For each gated project emit `    section {mermaid_gantt_label(project)}` then `    {mermaid_gantt_label(str(meta.get("sprint","Sprint")))} :active, {s_start}, {s_end}`. Close the fence. (Always emit the gantt fence/header even if no project qualifies — a header-only gantt is valid; keep it simple.)

`## Sprint Summary` — a Markdown table: header `| Project | Sprint | Window | Total Effort | % Done |` and separator `| :--- | :--- | :--- | :---: | :---: |`. Per project: `sprint = meta.get("sprint","-")`; window = `f"{s_start} → {s_end}"` when both meta sprint_start/sprint_end are truthy else `"-"` (use raw meta values, not the ISO-gated locals — windows may show placeholders, that's fine, escape via `_md_cell`); `total_days = sum(parse_effort_days(t["effort"]) for t in tasks)`; `done_days = sum(parse_effort_days(t["effort"]) for t in tasks if t["status"]=="Done")`; `pct = round(done_days/total_days*100, 1) if total_days else 0`. Emit `f"| {_md_cell(project)} | {_md_cell(str(sprint))} | {_md_cell(window)} | {total_days}d | {pct}% |"`. Accumulate portfolio totals; after the loop emit a TOTAL row: `f"| **TOTAL** | | | **{grand_total}d** | **{overall_pct}%** |"` where `overall_pct = round(grand_done/grand_total*100,1) if grand_total else 0`. All free-text cells go through `_md_cell`.

(B) Wire into `main()` (aggregator.py ~847-849): after the dependency-graph `write_text(...)` + its print, add:
`(DOCS_DIR / "agile-sprints.md").write_text(generate_agile_sprints(data))` and `print("Generated agile-sprints.md")`.

(C) In `docs/_includes/sidebar.html`, after the migration-gantt `<li>` (lines ~58-63), add a new `<li>` mirroring the existing markup exactly: `<a href="{{ '/agile-sprints.html' | relative_url }}" {% if page.url contains 'agile-sprints' %}aria-current="page"{% endif %}>Agile Sprints</a>` (keep the same indentation/whitespace/aria-current pattern as the Migration Gantt link).
  </action>
  <verify>
    <automated>cd /Users/machina/Dev/kf-cpto &amp;&amp; python -c "
import sys; sys.path.insert(0,'scripts')
import aggregator as a
data={'R3-AAS':{'meta':{'sprint':'S1','sprint_start':'2026-07-01','sprint_end':'2026-07-10'},'tasks':[{'task':'t','assignee':'x','effort':'3d','status':'Done','start':'','end':''},{'task':'t2','assignee':'x','effort':'1d','status':'Todo','start':'','end':''}]},'kf-platform':{'meta':{'sprint':'S2','sprint_start':'TBD','sprint_end':'-'},'tasks':[{'task':'u','assignee':'y','effort':'2d','status':'Done','start':'','end':''}]}}
s=a.generate_agile_sprints(data)
assert 'title: Agile Sprints' in s, 'frontmatter title missing'
assert '\`\`\`mermaid' in s and 'gantt' in s, 'gantt block missing'
assert '## Sprint Summary' in s, 'summary table missing'
assert '**TOTAL**' in s, 'TOTAL row missing'
assert 'section R3-AAS' in s, 'gated project section missing'
print('T4 OK')
"</automated>
    <automated>cd /Users/machina/Dev/kf-cpto &amp;&amp; grep -q "agile-sprints.html" docs/_includes/sidebar.html &amp;&amp; grep -q 'DOCS_DIR / \"agile-sprints.md\"' scripts/aggregator.py &amp;&amp; echo "T4 wiring OK"</automated>
  </verify>
  <done>`generate_agile_sprints` returns a page with `title: Agile Sprints`, a mermaid gantt, and a Sprint Summary table ending in a TOTAL row; `main()` writes `docs/agile-sprints.md`; the sidebar has an Agile Sprints nav link.</done>
</task>

</tasks>

<verification>
After all tasks, the module must parse cleanly:

```
cd /Users/machina/Dev/kf-cpto && python -c "import ast; ast.parse(open('scripts/aggregator.py').read())"
```

All four per-task verify snippets pass (T1 OK / T2 OK / T3 OK / T4 OK / T4 wiring OK).
</verification>

<success_criteria>
- Project page links resolve via lowercase paths; visible labels and builder `?project=` stay original-case.
- LOE report has no Effort-by-Assignee section.
- Calendar shows a computed Effort-by-Project pie (descending, non-zero only); no static CPTO pie; Sprint Calendar gantt intact.
- Per-project pages have no Effort Distribution pie.
- Agile Sprints page generates (timeline gantt + summary table with TOTAL row), is written by `main()`, and is linked in the sidebar.
- `scripts/aggregator.py` parses (ast clean); deterministic; no second parser; no `loe.yml` change.
</success_criteria>

<output>
Create `.planning/quick/260626-b6m-dashboard-fixes-lowercase-project-links-/260626-b6m-SUMMARY.md` when done.
</output>
