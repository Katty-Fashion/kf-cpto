---
id: 260707-ni6
type: quick
title: Wire validate_okf into CI (non-blocking) + document OKF in README
branch: master
autonomous: true
---

## Objective

Wire `scripts/validate_okf.py` into the `aggregate.yml` CI pipeline as a non-blocking step, and insert OKF documentation into `README.md` (one overview bullet + a full `## Knowledge Bundle (OKF)` section).

## Tasks

### Task 1 — Wire validate_okf into CI (non-blocking)

In `.github/workflows/aggregate.yml`, insert a new step immediately AFTER the "Validate Mermaid diagram syntax" step and BEFORE the "Commit unified docs" step:

```yaml
      - name: Validate OKF bundle (non-blocking)
        # OKF is a secondary, additive artifact. Surface conformance issues in
        # the log without ever blocking the canonical Pages deploy.
        continue-on-error: true
        run: python scripts/validate_okf.py
```

Match surrounding 6-space indentation. `continue-on-error: true` ensures a malformed bundle never blocks the canonical Pages deploy.

### Task 2 — Document OKF in README.md

2a. Add one bullet to the Overview feature list (after the "Activity Sync Skill" bullet):

```
- **Knowledge Bundle (OKF)** — An agent-consumable [Open Knowledge Format](...) bundle ...
```

2b. Insert a full `## Knowledge Bundle (OKF) — Agent-Consumable Project Memory` section after "## How It Works" and before "## Zero-Config Repo Registration".

## Constraints

- Non-blocking CI step: `continue-on-error: true` is mandatory.
- Verify: `python scripts/validate_okf.py` exits 0 after edit.
- Verify: YAML parses after aggregate.yml edit.
- Atomic commits: (a) aggregate.yml, (b) README.md.
- Do NOT commit PLAN.md/SUMMARY.md. Do NOT update ROADMAP.md. Do NOT push.
