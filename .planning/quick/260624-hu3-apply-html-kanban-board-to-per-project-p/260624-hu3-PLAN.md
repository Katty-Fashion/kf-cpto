---
phase: quick-260624-hu3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/aggregator.py
  - docs/assets/css/custom.css
  - scripts/sheets_sync.py
autonomous: true
requirements: [HU3-01, HU3-02, HU3-03]

must_haves:
  truths:
    - "Per-project pages render the HTML kanban board (no Mermaid kanban fence, no status pills)"
    - "Per-project cards show task text only (no project prefix) and have no link"
    - "Unified kanban output is byte-identical to before (linked cards retained)"
    - "The status-pill / status-legend system is fully removed from code and CSS"
    - "DEFAULT_SUMMARY_SHEET_ID is the new native sheet id"
  artifacts:
    - path: "scripts/aggregator.py"
      provides: "_render_kanban_board with link_project flag; _status_legend deleted"
      contains: "link_project"
    - path: "docs/assets/css/custom.css"
      provides: ".kanban-card--static rule; status-pill rules removed"
      contains: "kanban-card--static"
    - path: "scripts/sheets_sync.py"
      provides: "Updated native summary sheet id"
      contains: "1jLa-1Kh49ewIuPErPmIzp2dBLoGxxEvn4Fz3z_cxAaY"
  key_links:
    - from: "generate_project_page"
      to: "_render_kanban_board"
      via: "call with link_project=False"
      pattern: "_render_kanban_board\\(statuses, link_project=False\\)"
    - from: "generate_unified_kanban"
      to: "_render_kanban_board"
      via: "call with default link_project=True"
      pattern: "_render_kanban_board\\(statuses\\)"
---

<objective>
Apply the existing HTML/CSS kanban board to per-project pages, remove the now-dead
status-pill system (helper + CSS), and update the stale local-default summary sheet id.

Purpose: Per-project pages still emit a Mermaid `kanban` block plus a colored pill
legend; after a prior task moved the unified kanban to an HTML board, the per-project
treatment is inconsistent and the pill system is dead weight everywhere.
Output: Generalized `_render_kanban_board`, deleted pill code/CSS, new sheet id constant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Current state extracted from the codebase. Use directly — no exploration needed. -->

scripts/aggregator.py — existing _render_kanban_board (unified-only signature):
- `def _render_kanban_board(statuses: dict) -> str:`
- Iterates `statuses.items()`; per column emits `kanban-col`, `kanban-col__head` with count.
- Card line (current, link form):
  `f'    <a class="kanban-card" href="{href}">{escaped_project}: {escaped_task}</a>'`
  where `href = "{{{{ '/projects/{proj}/' | relative_url }}}}".format(proj=project)`.
- `_html_escape(s)` wraps `html.escape(s, quote=True)`.

scripts/aggregator.py — generate_unified_kanban caller (line ~133):
- `lines.append(_render_kanban_board(statuses))` — MUST stay default (link_project=True).

scripts/aggregator.py — _status_legend (line ~53, to DELETE):
- Builds `<div class="status-legend">` of `<span class="status-pill status-pill--{slug}">`.

scripts/aggregator.py — generate_project_page kanban section (lines ~360-389):
- After the `## Current Sprint Kanban … [Edit Kanban] … [raw]` header:
  - `lines.append(_status_legend())` (line 367) — REMOVE.
  - `lines.append("```mermaid")` / `lines.append("kanban")` … grouping loop building
    `statuses = {STATUS_TO_MERMAID[s]: [] for s in TASK_STATUSES}` with `id` `f"t{n}"`,
    then emitting `f'    {task["id"]}["{mermaid_label_safe(task["task"])}"]'`, then
    closing ```` ``` ```` (lines 369-389) — REPLACE with the HTML board call.
- Task Summary table, LOE summary, Sprint Timeline gantt follow — leave unchanged.

docs/assets/css/custom.css:
- Lines ~203-226: `.status-legend`, `.status-pill`, `.status-pill--{todo,in-progress,review,done}` — DELETE the whole `/* ===== Status Pills ===== */` block.
- Lines ~296-313: base `.kanban-card` (display:block; bg #fff; border; padding; etc.) and `.kanban-card:hover` — reuse for the static variant.

scripts/sheets_sync.py line 69:
- `DEFAULT_SUMMARY_SHEET_ID = "11hdbqxDl-9MVEEUovS_jpGJSe52TSy19"  # R3Group sheet`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-project HTML kanban board via generalized _render_kanban_board</name>
  <files>scripts/aggregator.py</files>
  <action>
Generalize `_render_kanban_board` to serve both callers (HU3-01). Change its signature to
`def _render_kanban_board(statuses: dict, *, link_project: bool = True) -> str:` and branch
only on the card line:
- When `link_project=True` (default, unified caller): emit the EXACT existing line —
  `f'    <a class="kanban-card" href="{href}">{escaped_project}: {escaped_task}</a>'`
  with `href` built as today. Keep computing `escaped_project` only in this branch.
- When `link_project=False`: emit
  `f'    <div class="kanban-card kanban-card--static">{escaped_task}</div>'`
  — no href, no project prefix; `escaped_task` still via `_html_escape`.
All column/head/count scaffolding stays shared. Do NOT change the unified caller at line
~133 (it keeps calling `_render_kanban_board(statuses)`); its output must be byte-identical.

In `generate_project_page` (lines ~360-389): remove the `lines.append(_status_legend())`
line. Keep the grouping loop that builds `statuses = {STATUS_TO_MERMAID[s]: [] for s in
TASK_STATUSES}` (the board needs the same dict shape; the per-task `id` field is now unused
but harmless — you may drop the `"id": f"t{n}"` to keep it clean, still appending
`{**task}`). Replace the `lines.append("```mermaid")` … `lines.append("```")` block
(including the `kanban` line and the `t{n}[...]` node emission) with
`lines.append(_render_kanban_board(statuses, link_project=False))` followed by
`lines.append("")`. Leave Task Summary, LOE summary, and Sprint Timeline gantt untouched.

Preserve all CLAUDE.md conventions: snake_case, type hints, list+join building, deterministic
output. Do NOT delete `_status_legend` yet — Task 2 removes it after confirming zero callers.
  </action>
  <verify>
    <automated>cd /Users/machina/Dev/kf-cpto && python3 -c "import ast,sys; ast.parse(open('scripts/aggregator.py').read()); sys.path.insert(0,'scripts'); import aggregator as a; pp=a.generate_project_page('demo', {'tasks':[{'task':'Build X','assignee':'A','effort':'2d','status':'In Progress'},{'task':'Ship Y','assignee':'B','effort':'1d','status':'Done'}],'frontmatter':{}}); assert 'status-pill' not in pp and 'status-legend' not in pp, 'pills leaked'; assert 'mermaid' not in pp.split('Sprint Timeline')[0] or '\`\`\`mermaid\nkanban' not in pp, 'kanban mermaid fence present'; assert 'kanban-card--static' in pp and 'kanban-board' in pp, 'no static board'; assert 'href=' not in pp.split('Task Summary')[0].split('kanban-board')[1], 'card has href'; assert 'Build X' in pp and 'demo: Build X' not in pp, 'project prefix or missing text'; assert 'Task Summary' in pp and 'LOE' in pp.upper() and 'Sprint Timeline' in pp, 'section dropped'; print('OK')"</automated>
  </verify>
  <done>Per-project page renders HTML board with static (un-linked, un-prefixed) cards, no pills, no mermaid kanban fence; Task Summary + LOE + Sprint Timeline retained; aggregator.py parses.</done>
</task>

<task type="auto">
  <name>Task 2: Delete dead pill code and CSS, add static-card style</name>
  <files>scripts/aggregator.py, docs/assets/css/custom.css</files>
  <action>
Remove the dead pill system now that no page emits pills (HU3-02). First confirm zero callers
of `_status_legend` remain after Task 1 (grep). Then delete the entire `_status_legend`
function (lines ~53-59) from `scripts/aggregator.py`.

In `docs/assets/css/custom.css`: delete the whole `/* ===== Status Pills ===== */` block —
`.status-legend`, `.status-pill`, and all four `.status-pill--*` rules (lines ~203-226),
including the section comment and the palette comment lines.

Add a minimal `.kanban-card--static` rule near the existing `.kanban-card` rules (after
`.kanban-card:hover`, ~line 313). It must drop the link affordance: set `cursor: default;`
and neutralize hover by adding `.kanban-card--static:hover { background:#fff; border-color:
rgba(0,0,0,0.12); }` (matching the base, non-hover look). Reuse the base `.kanban-card` block
for everything else — do NOT re-declare padding/border/background on the static class beyond
what is needed to kill the hover/pointer affordance.
  </action>
  <verify>
    <automated>cd /Users/machina/Dev/kf-cpto && grep -rn "_status_legend" scripts/ && echo "FAIL: callers remain" && exit 1; grep -n "status-pill\|status-legend" docs/assets/css/custom.css && echo "FAIL: pill css remains" && exit 1; grep -q "kanban-card--static" docs/assets/css/custom.css && python3 -c "import ast; ast.parse(open('scripts/aggregator.py').read()); print('OK')"</automated>
  </verify>
  <done>`_status_legend` fully removed (zero grep hits); no `status-pill`/`status-legend` in CSS; `.kanban-card--static` rule present; aggregator.py parses.</done>
</task>

<task type="auto">
  <name>Task 3: Update DEFAULT_SUMMARY_SHEET_ID to native sheet id</name>
  <files>scripts/sheets_sync.py</files>
  <action>
Update the stale local-run default summary sheet id (HU3-03). On line ~69 change
`DEFAULT_SUMMARY_SHEET_ID = "11hdbqxDl-9MVEEUovS_jpGJSe52TSy19"  # R3Group sheet`
to use the new native id `"1jLa-1Kh49ewIuPErPmIzp2dBLoGxxEvn4Fz3z_cxAaY"` and adjust the
trailing comment to note it is the native Google Sheet, e.g.
`# R3Group sheet (native Google Sheet)`. This is the local-default only; CI uses the
GSHEET_SUMMARY_ID secret. Do NOT touch any `.planning/` historical docs that mention the old
id. This is a pure constant change — the sheets_sync exit-0 invariant is unaffected.
  </action>
  <verify>
    <automated>cd /Users/machina/Dev/kf-cpto && grep -n DEFAULT_SUMMARY_SHEET_ID scripts/sheets_sync.py | grep -q "1jLa-1Kh49ewIuPErPmIzp2dBLoGxxEvn4Fz3z_cxAaY" && ! grep -q "11hdbqxDl-9MVEEUovS_jpGJSe52TSy19" scripts/sheets_sync.py && python3 -c "import ast; ast.parse(open('scripts/sheets_sync.py').read()); print('OK')"</automated>
  </verify>
  <done>Constant holds the new native id, old id absent from sheets_sync.py, file parses.</done>
</task>

</tasks>

<verification>
- Unified output unchanged: `generate_unified_kanban` still emits linked `.kanban-card`
  with `{{ '/projects/.../' | relative_url }}` and `{project}: {task}` text.
- Per-project output: `.kanban-board` + `.kanban-card--static` present; no href on cards;
  full task text with no project prefix; no `status-pill`/`status-legend`; no mermaid kanban.
- `grep -rn "_status_legend" scripts/` returns nothing.
- `grep -n "status-pill" docs/assets/css/custom.css` returns nothing.
- `grep -n DEFAULT_SUMMARY_SHEET_ID scripts/sheets_sync.py` shows the new native id.
- `ast.parse` clean for both modified .py files.
</verification>

<success_criteria>
Three atomic commits (board, pill-removal+css, sheet-id). Per-project pages use the shared
HTML board with static cards; unified output byte-identical; pill system gone from code and
CSS; native sheet id in place; all conventions preserved; loe.yml and exit-0 invariant untouched.
</success_criteria>

<output>
Create `.planning/quick/260624-hu3-apply-html-kanban-board-to-per-project-p/260624-hu3-SUMMARY.md` when done.
</output>
