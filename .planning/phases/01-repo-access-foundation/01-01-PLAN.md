---
phase: 01-repo-access-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .claude/skills/activity-sync/SKILL.md
autonomous: true
requirements: [REPO-01]
must_haves:
  truths:
    - "Files created under .claude/skills/ are visible to git (appear in git status / git add -A dry-run)"
    - "repos-local/ is ignored by git (does not appear in git add -A dry-run)"
    - "The activity-sync skill is discoverable: SKILL.md exists with valid frontmatter"
  artifacts:
    - path: ".gitignore"
      provides: "Fine-grained .claude exclusions that preserve .claude/skills/; repos-local/ ignored"
      contains: "repos-local/"
    - path: ".claude/skills/activity-sync/SKILL.md"
      provides: "Claude Code skill index for activity-sync"
      min_lines: 10
  key_links:
    - from: ".gitignore"
      to: ".claude/skills/activity-sync/"
      via: "fine-grained exclusion (blanket .claude/ removed)"
      pattern: "skills"
---

<objective>
Unblock and scaffold the activity-sync skill. The existing `.gitignore` has a blanket `.claude/` exclusion (line 69) that makes ALL skill code invisible to git — `git add -A` would add nothing. This plan replaces that blanket entry with fine-grained exclusions so `.claude/skills/` is tracked while Claude session files stay ignored, adds `repos-local/` to `.gitignore` (mirrors the existing `repos/` entry), and creates the skill's `SKILL.md` index.

This is the enabling slice: no subsequent skill code (bootstrap.py, repo_enum.py) can be committed until the gitignore conflict is resolved. After this plan, the skill directory exists and is git-trackable.

Purpose: Resolve the blocking gitignore conflict (RESEARCH Pitfalls 3 + 4) and stand up the skill scaffold so later waves can add committable Python modules.
Output: Modified `.gitignore`, new `.claude/skills/activity-sync/SKILL.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repo-access-foundation/01-RESEARCH.md
@.planning/phases/01-repo-access-foundation/01-PATTERNS.md

<interfaces>
<!-- Current .gitignore relevant lines (verified 2026-06-04): -->
<!-- line 4-5:  "# Cloned project repos (runtime)" / "repos/" -->
<!-- line 68-69: "#Claude" / ".claude/"  <- the blanket exclusion to REPLACE -->

<!-- Git negation fact (VERIFIED): "!.claude/skills/" does NOT work while the -->
<!-- parent ".claude/" is excluded. The blanket entry MUST be replaced, not supplemented. -->

<!-- Required new .gitignore state (PATTERNS.md "Required change"): -->
<!--   # Claude session files (exclude; not skill code)        -->
<!--   .claude/settings.local.json                             -->
<!--   .claude/cache/                                          -->
<!--   .claude/memory/                                         -->
<!--   # Runtime dirs (skill-local; never committed)           -->
<!--   repos-local/                                            -->

<!-- SKILL.md frontmatter fields (name, description, allowed-tools) are [ASSUMED] from -->
<!-- prior STACK.md research (RESEARCH Assumption A1). Verify against current Claude Code -->
<!-- skills spec via WebFetch before writing; if fields differ, use the verified field names. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace blanket .claude/ exclusion and add repos-local/ to .gitignore</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (the file being modified; current line 68-69 blanket .claude/ and line 4-5 repos/ entry)
    - .planning/phases/01-repo-access-foundation/01-RESEARCH.md (Pitfalls 3 and 4 — gitignore conflict and negation gotcha)
    - .planning/phases/01-repo-access-foundation/01-PATTERNS.md (".gitignore (config, modification)" section — exact required-change block)
  </read_first>
  <action>
    Edit `.gitignore`. Remove the blanket `.claude/` line (currently line 69) and its `#Claude` header (line 68). In its place add a fine-grained Claude-session-files block excluding `.claude/settings.local.json`, `.claude/cache/`, and `.claude/memory/` (these are session artifacts, NOT skill code). Then add a runtime-dirs block with `repos-local/` (mirrors the existing `repos/` entry at line 5; satisfies REPO-01 / criterion 3 that repos-local/ stays untracked). Do NOT use a `!.claude/skills/` negation — it is silently ignored while a parent is excluded (RESEARCH Pitfall 4); the blanket entry must be replaced, not supplemented.
  </action>
  <verify>
    <automated>cd /Users/machina/Dev/kf-cpto && grep -q '^repos-local/$' .gitignore && ! grep -qE '^\.claude/$' .gitignore && mkdir -p .claude/skills/activity-sync repos-local && touch .claude/skills/activity-sync/_probe.txt repos-local/_probe.txt && git add -An | grep -q '\.claude/skills/activity-sync/_probe.txt' && ! (git add -An | grep -q 'repos-local/') && rm -f .claude/skills/activity-sync/_probe.txt repos-local/_probe.txt && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` no longer contains a standalone `.claude/` blanket line.
    - `.gitignore` contains `repos-local/`.
    - `git add -An` (dry-run) lists a probe file created under `.claude/skills/activity-sync/`.
    - `git add -An` (dry-run) does NOT list any path under `repos-local/`.
    - No `!.claude/skills/` negation line is present.
  </acceptance_criteria>
  <done>Skill code under .claude/skills/ is git-trackable; repos-local/ is git-ignored; verification command prints PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Create the activity-sync SKILL.md index</name>
  <files>.claude/skills/activity-sync/SKILL.md</files>
  <read_first>
    - .planning/phases/01-repo-access-foundation/01-RESEARCH.md (Code Examples — "SKILL.md Minimal Frontmatter"; Assumption A1)
    - .planning/phases/01-repo-access-foundation/01-PATTERNS.md (".claude/skills/activity-sync/SKILL.md (config)" section)
    - CLAUDE.md ("Project Skills" section — discovery paths)
  </read_first>
  <action>
    First verify the current Claude Code skills SKILL.md frontmatter spec (field names `name`, `description`, `allowed-tools` are [ASSUMED] per RESEARCH A1) using WebFetch against the official Claude Code skills documentation; if the verified field names differ, use the verified names. Create `.claude/skills/activity-sync/SKILL.md` with YAML frontmatter (`name: activity-sync`, a `description` that states read-only enumeration/parsing of tracked sibling repos and trigger phrases like "activity sync, repo enum, list tracked repos", and `allowed-tools: Bash Read`) followed by a body titled "Activity Sync — Repo Access" that documents the two run commands: `python .claude/skills/activity-sync/bootstrap.py` (first-run clone + seed) and `python .claude/skills/activity-sync/repo_enum.py` (enumerate + fetch + parse). Reference the scripts by path only — they are created in later waves; this file is the index/contract. Use `[LABEL]` text pills, never emojis (CLAUDE.md convention).
  </action>
  <verify>
    <automated>cd /Users/machina/Dev/kf-cpto && test -f .claude/skills/activity-sync/SKILL.md && head -1 .claude/skills/activity-sync/SKILL.md | grep -q '^---$' && grep -q 'name: activity-sync' .claude/skills/activity-sync/SKILL.md && grep -q 'repo_enum.py' .claude/skills/activity-sync/SKILL.md && grep -q 'bootstrap.py' .claude/skills/activity-sync/SKILL.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/skills/activity-sync/SKILL.md` exists and opens with a `---` YAML frontmatter fence.
    - Frontmatter declares `name: activity-sync` and a description with trigger phrases (or the verified-spec equivalent field names).
    - Body references both `repo_enum.py` and `bootstrap.py` by their `.claude/skills/activity-sync/` path.
    - No emojis appear; status/label prose uses `[LABEL]` pills.
  </acceptance_criteria>
  <done>SKILL.md exists with valid frontmatter and documents both skill entry points; verification command prints PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git index ← .gitignore rules | A mis-scoped exclusion can either leak secrets (over-inclusion) or silently drop skill code (over-exclusion). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .gitignore fine-grained `.claude/` rules | mitigate | Explicitly exclude `.claude/settings.local.json`, `.claude/cache/`, `.claude/memory/` (session secrets) while un-blanketing the dir; verify dry-run does NOT add session files. |
| T-01-02 | Tampering (silent code loss) | `.claude/skills/` trackability | mitigate | Replace blanket entry (not `!` negation); verify probe file under `.claude/skills/` appears in `git add -An`. |
| T-01-03 | Information Disclosure | `repos-local/` clones committed | mitigate | Add `repos-local/` to `.gitignore`; verify probe under `repos-local/` is NOT staged. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (stdlib + existing pyyaml only per RESEARCH "No Package Legitimacy Audit Required"). |
</threat_model>

<verification>
- `git add -An` adds `.claude/skills/` content but not `repos-local/` content.
- `.gitignore` has no standalone `.claude/` blanket line and no `!.claude/skills/` negation.
- SKILL.md exists with valid frontmatter referencing both scripts.
</verification>

<success_criteria>
- Skill code under `.claude/skills/` is committable (criterion 3 enabler — gitignore confirmed).
- `repos-local/` is ignored (criterion 3).
- The activity-sync skill scaffold is discoverable via SKILL.md.
</success_criteria>

<output>
Create `.planning/phases/01-repo-access-foundation/01-01-SUMMARY.md` when done.
</output>
