---
phase: 01-repo-access-foundation
plan: 04
type: execute
wave: 4
depends_on: [01-01, 01-02, 01-03]
files_modified: [.claude/skills/activity-sync/repo_enum.py]
autonomous: true
gap_closure: true
requirements: [REPO-01]

must_haves:
  truths:
    - "Running repo_enum.py skips any repos-local/ entry that is a valid git repo but lacks kanban.md OR lacks .github/workflows/notify-kf-cpto.yml, logging a Warning"
    - "All 6 currently-cloned tracked repos still enumerate (happy path not regressed)"
    - "A marker-less git repo placed under repos-local/ is skipped and absent from the enumerated set"
  artifacts:
    - path: ".claude/skills/activity-sync/repo_enum.py"
      provides: "enumerate_repos() with runtime marker-presence check (kanban.md + notify-kf-cpto.yml)"
      contains: "missing required markers"
  key_links:
    - from: "enumerate_repos()"
      to: "repos-local/<name>/kanban.md and repos-local/<name>/.github/workflows/notify-kf-cpto.yml"
      via: "Path.exists() check after _is_git_repo() guard"
      pattern: "notify-kf-cpto.yml"
---

<objective>
Close the SC-1 / REPO-01 gap: enumerate_repos() currently includes any directory that passes _is_git_repo() without verifying the two required markers. ROADMAP SC-1 and REQUIREMENTS.md REPO-01 require enumeration to detect BOTH kanban.md AND .github/workflows/notify-kf-cpto.yml before including a repo in the tracked set.

Purpose: Add the missing runtime safety invariant so a manually-cloned or failed-seeding repo under repos-local/ is skipped (with a Warning) instead of silently processed. This makes the code satisfy the ROADMAP contract text, not just the current populated state.

Output: A surgically-patched enumerate_repos() that filters on marker presence, with no regression to the 6 real repos and no new static repo list.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repo-access-foundation/01-VERIFICATION.md
@.planning/phases/01-repo-access-foundation/01-CONTEXT.md

<interfaces>
Executor patches enumerate_repos() IN PLACE; do not rewrite the module.

Current enumerate_repos() (.claude/skills/activity-sync/repo_enum.py lines 186-205):
- Early return if not repos_local.exists() with a Warning.
- Iterates repos_local.iterdir(); skips non-dirs (continue).
- If _is_git_repo(str(entry)): appends entry.name. Else: prints "Warning: repos-local/{name} is not a valid git repo — skipping".
- Returns sorted(names).

Module constant already present: REPOS_LOCAL_DIR = _REPO_ROOT / "repos-local".

Python 3.9 constraints (already honored): from __future__ import annotations present; no PEP 604 unions at runtime, no match. Use print(f"Warning: ...") for non-fatal. NO static TRACKED_REPOS list may be introduced. Skill stays READ-ONLY (no writes/seeding here — seeding is bootstrap.py's job).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add marker-presence filter to enumerate_repos()</name>
  <read_first>
    .claude/skills/activity-sync/repo_enum.py (lines 186-205, enumerate_repos) — the function to patch.
    .planning/phases/01-repo-access-foundation/01-VERIFICATION.md (gaps block + Gaps Summary) — the exact required Warning text and skip semantics.
  </read_first>
  <files>.claude/skills/activity-sync/repo_enum.py</files>
  <action>
    Patch enumerate_repos() (lines 186-205) in place. Inside the `if _is_git_repo(str(entry)):` branch, BEFORE appending entry.name, add a marker-presence check (satisfies SC-1 / REPO-01):

    - Compute two paths from entry: kanban_marker = entry / "kanban.md" and notify_marker = entry / ".github" / "workflows" / "notify-kf-cpto.yml".
    - If NOT (kanban_marker.exists() AND notify_marker.exists()): emit exactly the line print(f"Warning: {entry.name} missing required markers (kanban.md + notify-kf-cpto.yml) — skipping") and continue (skip the entry — do NOT append).
    - Only when BOTH markers exist, append entry.name.

    Constraints (do NOT violate):
    - READ-ONLY: check existence only; never create/seed marker files here (seeding belongs to bootstrap.py per 01-CONTEXT decisions).
    - Do NOT introduce any static repo list — the function still scans repos_local.iterdir() at runtime.
    - Keep the existing non-git-dir Warning branch unchanged. Keep the not-repos_local.exists() early-return unchanged. Keep the trailing return sorted(names) unchanged.
    - Update the function docstring to note that entries lacking both markers are skipped (one sentence; do not reformat the rest).
    - Do NOT touch the org-origin check (_check_remote_org) or any other passing criterion.
  </action>
  <verify>
    <automated>cd /Users/machina/Dev/kf-cpto && grep -v '^#' .claude/skills/activity-sync/repo_enum.py | grep -c 'notify-kf-cpto'</automated>
  </verify>
  <acceptance_criteria>
    - enumerate_repos() checks entry/"kanban.md" AND entry/".github"/"workflows"/"notify-kf-cpto.yml" existence after the _is_git_repo() guard.
    - On a git repo missing either marker, it prints exactly: Warning: {name} missing required markers (kanban.md + notify-kf-cpto.yml) — skipping, and does NOT append the name.
    - No static repo list added; iteration still uses repos_local.iterdir().
    - The verify grep command returns a count of at least 1 (the failing gap signal was 0).
  </acceptance_criteria>
  <done>Marker-presence filter is present in enumerate_repos(); marker-less git repos are skipped with the exact Warning; no static list introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Prove skip-on-missing-marker plus happy-path no-regression</name>
  <read_first>
    .claude/skills/activity-sync/repo_enum.py (patched enumerate_repos and run()).
    .planning/phases/01-repo-access-foundation/gap-probe-marker-skip.sh (the prebuilt probe this task runs).
    .planning/phases/01-repo-access-foundation/01-VERIFICATION.md (Behavioral Spot-Checks — 6 repos enumerate, tree CLEAN).
  </read_first>
  <files>.claude/skills/activity-sync/repo_enum.py</files>
  <action>
    No code change unless Task 1 verification fails. This is the verification gate proving the gap is closed without regression. Run the prebuilt probe script gap-probe-marker-skip.sh (in this phase dir). It: (1) creates a throwaway marker-less git repo under repos-local/, asserts enumerate_repos() omits it, removes it; (2) asserts all 6 real tracked repos still enumerate; (3) runs a live python repo_enum.py expecting exit 0; (4) asserts git status is clean afterward. If the probe prints anything other than the final PASS line, return to Task 1 and fix the filter.
  </action>
  <verify>
    <automated>bash /Users/machina/Dev/kf-cpto/.planning/phases/01-repo-access-foundation/gap-probe-marker-skip.sh</automated>
  </verify>
  <acceptance_criteria>
    - The probe prints "OK skip+noregress:" with a names list that does NOT contain __gap_probe__ and DOES contain all 6 real repos (kf-be-platform, kf-fe-platform, kf-platform, R3-AAS, ai-rise-options, tech_brainstorming).
    - The live python repo_enum.py run exits 0.
    - git status --porcelain is empty after the run (kf-cpto tree CLEAN).
    - Final line printed is exactly: TREE CLEAN — gap probe PASSED.
  </acceptance_criteria>
  <done>Negative case (marker-less repo skipped) and positive case (all 6 enumerate, tree clean, exit 0) both pass via the probe. Gap SC-1 / REPO-01 closed.</done>
</task>

</tasks>

<verification>
- grep for the marker token in repo_enum.py returns >= 1 (Task 1): the runtime marker check exists.
- gap-probe-marker-skip.sh exits 0 and prints the PASS line (Task 2): skip works and the 6 real repos are not regressed; tree stays clean.
- No static repo list introduced: grep -c 'TRACKED_REPOS' .claude/skills/activity-sync/repo_enum.py returns 0.
</verification>

<success_criteria>
- enumerate_repos() skips any repos-local/ entry that is a valid git repo but lacks kanban.md or notify-kf-cpto.yml, logging the exact Warning.
- All 6 currently-cloned tracked repos still enumerate after the change.
- A throwaway marker-less git repo under repos-local/ is omitted from the enumerated set.
- kf-cpto working tree is clean after a live run; skill remains READ-ONLY; no static repo list added.
- REPO-01 marker-detection clause (SC-1) satisfied.
</success_criteria>

<output>
Create `.planning/phases/01-repo-access-foundation/01-04-SUMMARY.md` when done.
</output>
