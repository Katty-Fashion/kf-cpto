---
phase: 03-write-back-diagram-sanitization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - .gitignore
  - .claude/skills/activity-sync/manifests/.gitkeep
  - .claude/skills/activity-sync/sanitize.py
  - .claude/skills/activity-sync/writeback.py
  - .claude/skills/activity-sync/test_writeback.py
autonomous: true
requirements: [WB-01, DIAG-01, DIAG-02, DIAG-03]
must_haves:
  truths:
    - "Task-table cells with emojis or Mermaid-break chars (: ( ) \" # ; { } |) are sanitized to readable substitutions before write"
    - "Romanian diacritics ă/â/î/ș/ț pass through sanitization unchanged"
    - "Header row, separator row, prose, HTML comments, and frontmatter are never sanitized"
    - "Frontmatter round-trip preserves inline # comments, key order, and quoting (WB-01)"
    - "A reconstructed kanban.md with no proposed changes is byte-identical to the original (idempotent no-op)"
    - "sanitize_body run twice produces identical output (idempotent)"
  artifacts:
    - path: ".claude/skills/activity-sync/sanitize.py"
      provides: "sanitize_cell() + sanitize_body() pure transforms + _BREAK_MAP + _is_emoji()"
      contains: "def sanitize_cell"
    - path: ".claude/skills/activity-sync/writeback.py"
      provides: "split_kanban(), roundtrip_frontmatter(), reconstruct_kanban(), apply_status_change(), _content_changed() string builders (no git yet)"
      contains: "def reconstruct_kanban"
    - path: ".claude/skills/activity-sync/test_writeback.py"
      provides: "no-pytest test harness covering sanitize + round-trip + status-cell + idempotency"
      contains: "def check"
    - path: "requirements.txt"
      provides: "ruamel.yaml>=0.17 skill-local dependency"
      contains: "ruamel.yaml>=0.17"
    - path: ".gitignore"
      provides: "manifests/ runtime-artifact exclusion"
      contains: ".claude/skills/activity-sync/manifests/"
  key_links:
    - from: ".claude/skills/activity-sync/writeback.py"
      to: ".claude/skills/activity-sync/sanitize.py"
      via: "import sanitize_body"
      pattern: "from sanitize import|import sanitize"
    - from: ".claude/skills/activity-sync/writeback.py"
      to: "ruamel.yaml"
      via: "frontmatter round-trip"
      pattern: "from ruamel.yaml import YAML"
---

<objective>
Build the pure, git-free core of the write-back path: Mermaid sanitization (DIAG-01/02/03), comment-preserving frontmatter round-trip + targeted status-cell replacement (WB-01), and a byte-compare idempotency check — all producing a corrected-kanban.md STRING with zero git operations. Also lay the phase config groundwork: add `ruamel.yaml>=0.17` to requirements.txt and exclude the gitignored `manifests/` runtime dir.

Purpose: This is the testable foundation Plans 02 and 03 build on. Everything here is a pure function of input strings, so it can be exhaustively unit-tested with a no-pytest harness before any external write risk exists. MVP vertical slice: by the end of this plan, the skill can take a kanban.md string + a list of proposals and emit the corrected string — the inner half of the write loop, fully verified.

Output: `sanitize.py` (pure transforms), the string-builder functions in `writeback.py`, `test_writeback.py` (sanitize + round-trip + idempotency coverage), updated `requirements.txt` and `.gitignore`, and the gitignored `manifests/` directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-write-back-diagram-sanitization/03-CONTEXT.md
@.planning/phases/03-write-back-diagram-sanitization/03-RESEARCH.md
@.planning/phases/03-write-back-diagram-sanitization/03-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — no exploration required. -->

reconcile.Proposal dataclass (from .claude/skills/activity-sync/reconcile.py, consumed in Plan 02/03; status-cell builder here must match `task` + `new_status`):
```
@dataclass
class Proposal:
    repo: str
    task: str            # free-text task name (matches kanban.md 'task' cell, parts[1].strip())
    old_status: str
    new_status: str      # validated TASK_STATUSES member
    tier: int
    signal: str
    signal_url: Optional[str] = None
```

utils.py constants (import via sys.path injection, REPO-03 — do NOT add a second parser):
```
TASK_STATUSES = ("Todo", "In Progress", "Review", "Done")
```

sys.path injection block to reuse verbatim in writeback.py (from reconcile.py lines 35–40):
```
_REPO_ROOT = Path(__file__).parent.parent.parent.parent
_SCRIPTS_DIR = _REPO_ROOT / "scripts"
if str(_SCRIPTS_DIR) not in sys.path:
    sys.path.insert(0, str(_SCRIPTS_DIR))
from utils import ORG, TASK_STATUSES  # noqa: E402
```

Verified string-builder contracts (from 03-RESEARCH.md Patterns 1, 2, 3, 6 — verified byte-identical against templates/kanban.md):
- `split_kanban(content: str) -> tuple[str, str]` — regex `_FM_RE = re.compile(r'^---\n(.*?)\n---\n?', re.DOTALL)`; returns (frontmatter_str, body_str); raises ValueError if no frontmatter.
- `roundtrip_frontmatter(fm_str: str) -> str` — ruamel `YAML()` with `preserve_quotes = True`; dump via `io.StringIO`; guard trailing newlines with `.rstrip("\n") + "\n"` (Pitfall 5).
- `reconstruct_kanban(fm_str, body_str) -> str` — returns `"---\n" + roundtrip_frontmatter(fm_str) + "---\n" + body_str`.
- `apply_status_change(body_str, task_name, new_status) -> tuple[str, bool]` — split each line on `|`; match `parts[1].strip() == task_name`; replace `parts[-2]` (last data cell = Status) with `f" {new_status} "`; first-match-only, log `[WARN]` on duplicate task name (Pattern 2).
- `_content_changed(kanban_path, proposed) -> bool` — `Path(kanban_path).read_bytes() != proposed.encode("utf-8")` (Pattern 6).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config groundwork + failing end-to-end test scaffold</name>
  <files>requirements.txt, .gitignore, .claude/skills/activity-sync/manifests/.gitkeep, .claude/skills/activity-sync/test_writeback.py</files>
  <read_first>
    - requirements.txt (append-only — preserve existing 4 deps)
    - .gitignore (append-only — note the `.claude/*` + `!.claude/skills/` negation makes manifests/ trackable; Pitfall 6)
    - .claude/skills/activity-sync/test_reconcile.py (no-pytest harness: check()/PASS/FAIL counters, section delimiters, _make_bare_remote pattern from 03-PATTERNS.md)
  </read_first>
  <action>
    Append to requirements.txt a new section after the requests line: a comment `# Skill-local dependency (skill only; CI never installs this)` then `ruamel.yaml>=0.17`. Do NOT modify the existing four deps.
    Append to .gitignore after the `repos-local/` line: a comment `# Skill runtime artifacts (per-run manifests; never committed)` then `.claude/skills/activity-sync/manifests/`. This is required because the `!.claude/skills/` negation otherwise makes manifests/ trackable (Pitfall 6).
    Create `.claude/skills/activity-sync/manifests/.gitkeep` as an empty sentinel so the dir exists; verify the gitignore rule keeps it out of `git add -A`.
    Create `test_writeback.py` following the test_reconcile.py harness: `from __future__ import annotations`, `check(name, condition)` helper with global PASS/FAIL counters, section-delimited prints, and a summary block (`print(f"\n--- Results: {PASS} passed, {FAIL} failed ---")`; `if FAIL > 0: sys.exit(1)`). Import sanitize and writeback as siblings via `_SKILL_DIR` sys.path injection. Write the test ASSERTIONS NOW (RED phase) for: sanitize_cell substitutions, Romanian-diacritic preservation, emoji strip, sanitize_body header/separator skip + idempotency, split_kanban/reconstruct_kanban byte-identity for unmodified frontmatter, apply_status_change cell replacement (4-col and 6-col), and _content_changed. These tests reference functions not yet implemented — running the file now MUST fail with ImportError/AttributeError. That failing state is the RED gate for Tasks 2–3.
  </action>
  <verify>
    <automated>cd /Users/machina/Dev/kf-cpto && grep -q "ruamel.yaml>=0.17" requirements.txt && grep -q "manifests/" .gitignore && git add -A --dry-run 2>/dev/null | grep -v "^#" | grep -c "manifests/" | grep -qx 0 && echo CONFIG_OK</automated>
  </verify>
  <acceptance_criteria>
    - `requirements.txt` contains the exact line `ruamel.yaml>=0.17`; the original four deps are unchanged.
    - `.gitignore` contains the line `.claude/skills/activity-sync/manifests/`.
    - `git add -A --dry-run` output does NOT include any path under `manifests/` (gitignore confirmed; the verify command's grep -c returns 0).
    - `.claude/skills/activity-sync/manifests/.gitkeep` exists on disk.
    - `python .claude/skills/activity-sync/test_writeback.py` exits NON-ZERO (RED) — it imports sanitize/writeback functions that do not exist yet.
    - test_writeback.py contains `def check(` and a `--- Results:` summary print.
  </acceptance_criteria>
  <done>requirements.txt + .gitignore updated, manifests/.gitkeep created and confirmed-gitignored, test_writeback.py written with failing assertions for all sanitize + string-builder behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sanitize.py — pure Mermaid sanitization (DIAG-01/02/03)</name>
  <files>.claude/skills/activity-sync/sanitize.py</files>
  <read_first>
    - .claude/skills/activity-sync/test_writeback.py (the RED assertions from Task 1 are the spec)
    - .planning/phases/03-write-back-diagram-sanitization/03-RESEARCH.md (Pattern 3 — verified sanitize_cell/sanitize_body + _is_emoji ranges + Romanian codepoints)
    - scripts/utils.py (parse_kanban_tasks pipe-split + header/separator skip convention — reuse the same guard, do NOT add a parser)
  </read_first>
  <behavior>
    - sanitize_cell("Deploy: prod (v2)") strips per _BREAK_MAP -> "Deploy - prod v2" (`:`→` -`, `(`/`)` dropped), collapses double spaces, .strip()
    - sanitize_cell('Fix "bug" #42 | urgent; now') -> "Fix 'bug' 42 / urgent, now" (`"`→`'`, `#`→dropped, `|`→`/`, `;`→`,`)
    - sanitize_cell("Migrează ședința în țară â î") preserves ă/â/î/ș/ț verbatim (no change)
    - sanitize_cell("Ship 🚀 it ✅") strips emoji codepoints -> "Ship it"
    - sanitize_cell is idempotent: sanitize_cell(sanitize_cell(x)) == sanitize_cell(x)
    - sanitize_body skips header row (first cell == "Task") and separator row (":---" present) byte-for-byte
    - sanitize_body leaves non-pipe lines (prose, HTML comments, blank) unchanged
    - sanitize_body sanitizes data cells parts[1..-2], preserving leading/trailing pipe structure and EOL
    - sanitize_body is idempotent: second pass == first pass output
  </behavior>
  <action>
    Create `sanitize.py` with `from __future__ import annotations`, `import re`, `import unicodedata`. Define `_BREAK_MAP` (`:`→` -`, `"`→`'`, `|`→`/`, `;`→`,`, `(`/`)`/`{`/`}`/`#`→`""`) and `_HEADER_CELLS = frozenset({"Task", ":---"})` as module SCREAMING_SNAKE_CASE/underscore constants. Implement `_is_emoji(cp: int) -> bool` covering the verified Unicode emoji blocks from RESEARCH.md Pattern 3 (Emoticons 1F600–1F64F, Misc Symbols+Pictographs 1F300–1F5FF, Transport 1F680–1F6FF, Supplemental 1F700–1F9FF, Chess 1FA00–1FA6F, Pictographs Ext-A 1FA70–1FAFF, Misc Symbols 2600–26FF, Dingbats 2700–27BF, Variation Selectors FE00–FE0F, Regional Indicators 1F1E0–1F1FF, ZWJ 200D). Implement `sanitize_cell(text)`: iterate chars, skip emoji codepoints, map break chars via `_BREAK_MAP.get(c, c)`, then `re.sub(r"  +", " ", ...).strip()`. Implement `sanitize_body(body)`: splitlines(keepends=True); pass through non-pipe lines, header (`parts[1].strip()=="Task"`) and separator (`":---" in stripped`) rows unchanged; for data rows sanitize parts[1..-2] wrapping each as `f" {sanitize_cell(...)} "` and rejoin preserving parts[0], parts[-1], and EOL. This is the FIRST and only sanitization fence this phase (aggregator-side second fence is deferred to v2/Phase 5 — do NOT add it). sanitize.py is a pure library: NO print, NO git, NO sys.path injection.
  </action>
  <verify>
    <automated>cd /Users/machina/Dev/kf-cpto && python -c "import sys; sys.path.insert(0,'.claude/skills/activity-sync'); from sanitize import sanitize_cell; assert sanitize_cell('Ship 🚀: prod (v2)')=='Ship - prod v2', sanitize_cell('Ship 🚀: prod (v2)'); assert sanitize_cell('țară ședință â î')=='țară ședință â î'; print('SANITIZE_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `sanitize.py contains def sanitize_cell(` and `def sanitize_body(` and `def _is_emoji(`.
    - The inline assertion command above prints `SANITIZE_OK` (substitution + diacritic preservation + emoji strip).
    - `sanitize.py` contains NO `import subprocess`, NO `print(`, NO sys.path injection (pure library).
    - The sanitize-related sections of `python .claude/skills/activity-sync/test_writeback.py` now PASS (run after Task 3 for the full GREEN gate).
  </acceptance_criteria>
  <done>sanitize.py implements sanitize_cell + sanitize_body + _is_emoji; substitutions, Romanian preservation, emoji strip, header/separator skip, and idempotency all verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: writeback.py string builders — round-trip + status-cell + idempotency (WB-01)</name>
  <files>.claude/skills/activity-sync/writeback.py</files>
  <read_first>
    - .claude/skills/activity-sync/test_writeback.py (RED assertions for split_kanban/reconstruct_kanban/apply_status_change/_content_changed)
    - .claude/skills/activity-sync/reconcile.py (lines 1–40: shebang, docstring, __future__, imports, sys.path injection block to copy verbatim)
    - templates/kanban.md (the frontmatter # comments + 4-col/6-col table this must round-trip byte-identically)
    - .planning/phases/03-write-back-diagram-sanitization/03-RESEARCH.md (Patterns 1, 2, 6; Pitfall 5 trailing-newline guard)
  </read_first>
  <behavior>
    - split_kanban(content) returns (frontmatter_str, body_str); raises ValueError on content with no `---` frontmatter
    - reconstruct_kanban(fm, body) of an UNMODIFIED templates/kanban.md is byte-identical to the original file (ruamel preserves # comments, key order, quoting — WB-01)
    - apply_status_change(body, task, "Done") replaces only the last data cell of the matching row; returns (new_body, True)
    - apply_status_change works for both 4-col and 6-col tables (status is always parts[-2])
    - apply_status_change with task not present returns (body, False) unchanged
    - apply_status_change where current status already == new returns (body, False)
    - apply_status_change with duplicate task names updates first match only and prints a [WARN]
    - _content_changed(path, identical_string) returns False; with a differing string returns True
  </behavior>
  <action>
    Create `writeback.py` with the reconcile.py header conventions: shebang, module docstring describing the write-back role (consumes reconcile.run() proposals; this plan adds only the pure string builders — git/push lands in Plan 02), `from __future__ import annotations`, stdlib imports (`re`, `io.StringIO`, `pathlib.Path`, `typing.Any`), `from ruamel.yaml import YAML`, and the verbatim sys.path injection block importing `ORG, TASK_STATUSES` from utils (REPO-03 — writeback imports reconcile/utils, never a second parser). Add module constants `_FM_RE = re.compile(r'^---\n(.*?)\n---\n?', re.DOTALL)` and `COMMIT_MSG = "chore(kanban): reconcile task statuses from repo activity"`. Implement `split_kanban`, `roundtrip_frontmatter` (ruamel `YAML()`, `preserve_quotes = True`, dump to StringIO, apply `.rstrip("\n") + "\n"` guard per Pitfall 5), `reconstruct_kanban` (exact `"---\n" + rt_fm + "---\n" + body_str`), `apply_status_change` (line.split("|"); guard `len(parts) >= 4` and `line.startswith("|")`; match `parts[1].strip()==task_name`; skip if old==new; set `parts[-2]=f" {new_status} "`; first-match-only with `[WARN]` on duplicates), and `_content_changed` (read_bytes vs encode). Do NOT add git/subprocess/push/manifest code in this plan — those are Plan 02/03. Do NOT round-trip the body through ruamel (frontmatter only — Anti-Pattern).
  </action>
  <verify>
    <automated>cd /Users/machina/Dev/kf-cpto && python .claude/skills/activity-sync/test_writeback.py</automated>
  </verify>
  <acceptance_criteria>
    - `writeback.py contains def reconstruct_kanban(` and `def apply_status_change(` and `def split_kanban(`.
    - `writeback.py contains from ruamel.yaml import YAML` and the `from utils import` sys.path block.
    - `python .claude/skills/activity-sync/test_writeback.py` exits 0 (GREEN) — all sanitize + string-builder + idempotency assertions pass.
    - The round-trip test asserts `reconstruct_kanban(*split_kanban(orig)) == orig` for templates/kanban.md (byte-identical, WB-01).
    - `writeback.py` contains NO `subprocess`/`push`/`set-url` code yet (those are Plan 02).
  </acceptance_criteria>
  <done>writeback.py string builders implemented; test_writeback.py exits 0 with full coverage of sanitize, frontmatter round-trip (byte-identical), status-cell replacement (4/6-col), and content-change detection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted kanban task text → corrected file string | Task cell content originates in external tracked repos; flows into the string that will later be committed/pushed |
| repo kanban.md → ruamel.yaml load | Frontmatter YAML parsed by ruamel round-trip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | task cell text with `\|`/`:`/control chars breaking the pipe table or Mermaid | mitigate | sanitize_cell substitution map (`\|`→`/`, `:`→` -`, etc.) + emoji strip applied to data cells only (DIAG-01); idempotent so repeated runs converge |
| T-03-02 | Tampering | sanitization touching frontmatter/prose/AUTO markers it must not | mitigate | sanitize_body scopes strictly to data rows; header/separator/non-pipe lines pass byte-for-byte; frontmatter is round-tripped separately by ruamel, never sanitized (DIAG-02) |
| T-03-03 | Denial of Service | malformed/multi-line YAML frontmatter crashing ruamel load | accept | local-only skill; malformed frontmatter in a tracked repo is an authoring error surfaced as a ValueError; no live push occurs this plan |
| T-03-SC | Tampering | pip install of ruamel.yaml | accept | RESEARCH.md Package Legitimacy Audit verified ruamel.yaml: 12-yr PyPI history, 273 releases, Anthon van der Neut, no postinstall — disposition Approved [VERIFIED: PyPI registry] |
</threat_model>

<verification>
- `python .claude/skills/activity-sync/test_writeback.py` exits 0 (all sanitize + builder assertions pass).
- `git add -A --dry-run` shows no path under `manifests/`.
- `git status --porcelain` in kf-cpto shows only the intended new/changed files (no repos-local/, no manifests/ JSON).
</verification>

<success_criteria>
- DIAG-01/02/03: emoji + break-char sanitization works on data cells only; Romanian diacritics preserved; idempotent.
- WB-01: frontmatter round-trip preserves # comments/key order/quoting; reconstruct of unmodified file is byte-identical.
- ruamel.yaml>=0.17 in requirements.txt; manifests/ gitignored and confirmed out of `git add -A`.
- SC-4 (partial): byte-compare idempotency function in place (full no-op-on-rerun verified in Plan 02/03 with git).
</success_criteria>

<output>
Create `.planning/phases/03-write-back-diagram-sanitization/03-01-SUMMARY.md` when done.
</output>
